name: prime
base: core22
version: "1.0"

summary: Aplikacija za ocjenjivanje ispita
description: Studentski projekt

grade: devel
confinement: devmode

apps:
  prime:
    command: bin/python3 $SNAPCRAFT_PROJECT_DIR/gui_grader.py

parts:
  app:
    plugin: python
    source: .
    stage-packages:
      - python3-tk
