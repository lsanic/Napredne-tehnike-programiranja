name: prime
base: core22
version: "1.0"
summary: Aplikacija za ocjenjivanje ispita
description: Studentski projekt

grade: devel
confinement: devmode

apps:
  prime:
    command: gui_grader.py
    extensions: [gnome]
    plugs:
      - home
      - removable-media

parts:
  app:
    plugin: dump
    source: .
    stage-packages:
      - python3-tk
